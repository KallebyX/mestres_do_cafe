services:
  # Backend API - Flask Python
  - type: web
    name: mestres-cafe-api
    runtime: python
    region: oregon
    plan: free
    buildCommand: |
      cd apps/api
      pip install -r requirements.txt
    startCommand: |
      cd apps/api
      python app.py
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mestres-cafe-db
          property: connectionString
      - key: PORT
        value: 10000
      - key: API_PORT
        value: 10000
      - key: PYTHONPATH
        value: /opt/render/project/src/apps/api
      - key: CORS_ORIGINS
        value: https://mestres-cafe-web.onrender.com,https://mestres-cafe-web-*.onrender.com
      - key: MAIL_SERVER
        value: smtp.gmail.com
      - key: MAIL_PORT
        value: 587
      - key: MAIL_USE_TLS
        value: "true"
    healthCheckPath: /api/health
    autoDeploy: true

  # Frontend - React Vite
  - type: web
    name: mestres-cafe-web
    runtime: node
    region: oregon
    plan: free
    buildCommand: ./scripts/render-build.sh
    startCommand: |
      cd apps/web
      npx serve -c serve.json -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
      - key: VITE_API_URL
        value: https://mestres-cafe-api.onrender.com
      - key: PORT
        value: 10000
    autoDeploy: true

databases:
  - name: mestres-cafe-db
    databaseName: mestres_cafe_production
    user: mestres_cafe_user
    plan: free
    region: oregon
    postgresMajorVersion: "15"
