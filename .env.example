# Mestres do Café - Environment Configuration Template
# Copie este arquivo para .env e preencha com os valores reais

# === APLICAÇÃO ===
APP_NAME="Mestres do Café"
APP_VERSION="1.0.0"
NODE_ENV=development
FLASK_ENV=development

# === URLS E PORTAS ===
API_URL=http://localhost:5001/api
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:5001/api
VITE_APP_NAME="Mestres do Café"

# === BANCO DE DADOS ===
# Para desenvolvimento local (SQLite)
DATABASE_URL=sqlite:///mestres_cafe.db

# Para produção (PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database_name

# === SEGURANÇA ===
# IMPORTANTE: Gere uma chave secreta forte para produção
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
SECRET_KEY=your-app-secret-key-change-this-in-production

# === CORS ===
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# === MERCADO PAGO (PAGAMENTOS) ===
# Sandbox para testes
MERCADO_PAGO_SANDBOX=true
MERCADO_PAGO_ACCESS_TOKEN=your-mercado-pago-access-token
MERCADO_PAGO_CLIENT_ID=your-mercado-pago-client-id
MERCADO_PAGO_CLIENT_SECRET=your-mercado-pago-client-secret
PAYMENT_WEBHOOK_URL=https://your-domain.com/api/webhooks/mercado-pago

# === TESTES E2E ===
# Senha padrão para todos os usuários de teste (APENAS PARA TESTES)
TEST_PASSWORD=TestSenha123!
TEST_FRONTEND_URL=http://localhost:3000
TEST_API_URL=http://localhost:5001

# === LOGS E MONITORAMENTO ===
LOG_LEVEL=info
ENABLE_DEBUG_LOGS=false

# === EMAIL (OPCIONAL) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_USE_TLS=true

# === UPLOAD DE ARQUIVOS ===
MAX_UPLOAD_SIZE=10485760  # 10MB em bytes
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,pdf

# === CACHE E REDIS (OPCIONAL) ===
# REDIS_URL=redis://localhost:6379/0
# CACHE_TYPE=redis

# === LOCALIZAÇÃO ===
TIMEZONE=America/Sao_Paulo
LOCALE=pt_BR
CURRENCY=BRL

# === RENDER DEPLOYMENT (PRODUÇÃO) ===
# Estas variáveis são configuradas automaticamente no Render
# PORT (configurado automaticamente)
# DATABASE_URL (configurado automaticamente)
# PYTHON_VERSION=3.11
# NODE_VERSION=18